extends layout 

block dhead
  link(rel="stylesheet", href="/assets/css/style.css")

block content 
  .w3-card-4
    header.w3-container.w3-blue
      h1.no-margin.mb= cast.name
      h6.no-margin.mt= cast.id
    
    .w3-container
      h2 Filmes
      table.w3-table-all
        tr
          thead
            tr
              th Ano
              th Título
          tbody
            each film in lfilmes
              tr(data-id=`${film.id}`)
                td= film.year
                td= film.title

  footer.we-container.w3-blue
    address Generated by ENGWEB2025 class