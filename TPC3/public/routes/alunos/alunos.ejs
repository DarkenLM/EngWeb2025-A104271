<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8" />
    <link rel="icon" href="/assets/img/favicon.png" />
    <link rel="stylesheet" href="/assets/css/w3.css" />
    <title>Student Management</title>
</head>

<body>
    <div class="w3-card-4">

        <header class="w3-container w3-teal">
            <h1>Students List
                <a class="w3-btn w3-round w3-grey" href="/alunos/registo">+</a>
            </h1>

        </header>

        <div class="w3-container">
            <table class="w3-table-all">
                <tr>
                    <th>Id</th>
                    <th>Name</th>
                    <th>GitLink</th>
                    <th>Actions</th>
                </tr>

                <% for (const aluno of alunos) { %>
                    <tr>
                        <td><%= aluno.id %></td>
                        <td>
                            <a href="/alunos/<%= aluno.id %>">
                                <%= aluno.nome %>
                            </a>
                        </td>
                        <td><%= aluno.gitlink %></td>
                        <td>
                            [<a href="/alunos/edit/<%= aluno.id %>">Edit</a>]
                            [<a href="/alunos/delete/<%= aluno.id %>">Delete</a>]
                        </td>
                    </tr>
                <% } %>
            </table>
        </div>
        <footer class="w3-container w3-blue">
            <h5>Generated by RPCW2023 in <%= date %></h5>
        </footer>
    </div>
</body>

</html>