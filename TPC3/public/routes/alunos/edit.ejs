<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8" />
    <link rel="icon" href="/assets/img/favicon.png" />
    <link rel="stylesheet" href="/assets/css/w3.css" />
    <title>Student Form</title>
</head>

<body>
    <div class="w3-card-4">
        <header class="w3-container w3-purple">
            <h2>Student Form</h2>
        </header>

        <form class="w3-container" method="POST">
            <fieldset>
                <legend>Metadata</legend>
                <label>Id</label>
                <input class="w3-input w3-round" type="text" name="id" value="<%= aluno.id %>" readonly/>
                <label>Name</label>
                <input class="w3-input w3-round" type="text" name="nome" placeholder="<%= aluno.nome %>" value="<%= aluno.nome %>"/>
                <label>Git</label>
                <input class="w3-input w3-round" type="text" name="git" placeholder="<%= aluno.git %>" value="<%= aluno.git %>"/>
            </fieldset>

            <fieldset>
                <legend>TPC</legend>
                <% for(let i = 1; i < 9; i++) { %>
                    <% const tpc = `tpc${i}` %>
                    <input class="w3-check" type="checkbox" name="tpc<%= i %>" value="<%= tpc in aluno %>" />
                    <label>TPC<%= i %></label>
                <% } %>
            </fieldset>
            <br />
            <button class="w3-btn w3-purple w3-mb-2" type="submit">Edit</button>
        </form>

        <footer class="w3-container w3-purple">
            <h5>Generated by EngWeb2023 in <%= date %> - [<a href="..">Return</a>]</h5>
        </footer>
    </div>
</body>